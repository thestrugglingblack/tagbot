#name: Deploy to Azure
#
#on:
#  push:
#    branches: [main]
#  workflow_dispatch:
#
#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#    needs: [lint, test, build-and-push]
#    if: github.ref == 'refs/heads/main'
#
#    steps:
#     name: Deploy to Azure
#
#on:
#  push:
#    branches: [main]
#  workflow_dispatch:
#
#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#    needs: [lint, test, build-and-push]
#    if: github.ref == 'refs/heads/main'
#
#    steps:
#      - uses: actions/checkout@v3
#
#      - name: Login to Azure
#        uses: azure/login@v1
#        with:
#          creds: ${{ secrets.AZURE_CREDENTIALS }}
#
#      - name: Deploy to Azure Container Instances
#        uses: azure/aci-deploy@v1
#        with:
#          resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
#          dns-name-label: tagbot
#          image: thestrugglingblack/tagbot:latest
#          name: tagbot
#          location: 'eastus'
#          secure-environment-variables: |
#            DISCORD_TOKEN=${{ secrets.DISCORD_TOKEN }}
#            DB_CONNECTION=${{ secrets.DB_CONNECTION }}
#            REDIS_CONNECTION=${{ secrets.REDIS_CONNECTION }} - uses: actions/checkout@v3
#
#      - name: Login to Azure
#        uses: azure/login@v1
#        with:
#          creds: ${{ secrets.AZURE_CREDENTIALS }}
#
#      - name: Deploy to Azure Container Instances
#        uses: azure/aci-deploy@v1
#        with:
#          resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
#          dns-name-label: tagbot
#          image: thestrugglingblack/tagbot:latest
#          name: tagbot
#          location: 'eastus'
#          secure-environment-variables: |
#            DISCORD_TOKEN=${{ secrets.DISCORD_TOKEN }}
#            DB_CONNECTION=${{ secrets.DB_CONNECTION }}
#            REDIS_CONNECTION=${{ secrets.REDIS_CONNECTION }}